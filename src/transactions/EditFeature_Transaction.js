import jsTPS from "../common/jsTPS"

export default class EditFeature_Transaction extends jsTPS_Transaction {

    constructor(initStore, initIndex, initOldProps, initNewProps) {
        super();
    }
}